# Test whether an object is an `rcdiag_result`

Test whether an object is an `rcdiag_result`

## Usage

``` r
is_rcdiag_result(x)
```

## Arguments

- x:

  Any R object.

## Value

`TRUE` if `x` inherits from `"rcdiag_result"`, otherwise `FALSE`.
