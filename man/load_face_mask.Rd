% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{load_face_mask}
\alias{load_face_mask}
\title{Load a face mask from a PNG or JPEG image}
\usage{
load_face_mask(path, threshold = 0.5, invert = FALSE)
}
\arguments{
\item{path}{Path to a PNG or JPEG file. Reading PNG requires the
\code{png} package; reading JPEG requires \code{jpeg} (both Suggests).}

\item{threshold}{Numeric in \verb{[0, 1]}. Pixels with grayscale value
strictly greater than this are \code{TRUE}. Default \code{0.5}.}

\item{invert}{If \code{TRUE}, the mask is inverted (useful for
black-on-white masks). Default \code{FALSE}.}
}
\value{
Logical vector of length \code{prod(img_dims)}, column-major (the
convention \code{\link[=face_mask]{face_mask()}} also uses).
}
\description{
Reads an image, converts to grayscale, and thresholds to a logical
mask. White-on-black masks (the typical convention) become \code{TRUE}
inside the face region and \code{FALSE} outside.
}
\details{
Use this when you have a hand-crafted or anatomically tuned mask and
want to feed it to \code{\link[=diagnose_infoval]{diagnose_infoval()}} or \code{\link[=infoval]{infoval()}} via the \code{mask}
argument. For the standard Schmitz 2024 oval, \code{\link[=face_mask]{face_mask()}} is faster
and adds no dependencies.
}
\seealso{
\code{\link[=face_mask]{face_mask()}}, \code{\link[=diagnose_infoval]{diagnose_infoval()}}.
}
