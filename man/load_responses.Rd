% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_loaders.R
\name{load_responses}
\alias{load_responses}
\title{Load reverse correlation response data from a CSV file}
\usage{
load_responses(
  path,
  method = c("2ifc", "briefrc"),
  col_participant = "participant_id",
  col_stimulus = "stimulus",
  col_response = "response",
  col_rt = NULL
)
}
\arguments{
\item{path}{Path to a CSV file.}

\item{method}{Either \code{"2ifc"} or \code{"briefrc"}. Currently informational
only; downstream checks use this to select method-specific logic.}

\item{col_participant, col_stimulus, col_response}{Column names that must
exist in the file. Default to the package-wide standard names.}

\item{col_rt}{Optional column name for response time. If provided, must
exist in the file.}
}
\value{
A \link[data.table:data.table]{data.table::data.table} with all columns from the CSV.
}
\description{
Reads a trial-level response CSV and returns a \link[data.table:data.table]{data.table::data.table}.
The function validates that the required columns are present, but does not
coerce column types or change column names.
}
\examples{
\dontrun{
responses <- load_responses("my_data.csv", method = "2ifc")
}

}
