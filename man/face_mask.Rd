% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/face_mask.R
\name{face_mask}
\alias{face_mask}
\title{Oval face-region mask for a square image}
\usage{
face_mask(
  img_dims,
  region = c("full", "eyes", "nose", "mouth", "upper_face", "lower_face"),
  centre = c(0.5, 0.5),
  half_width = 0.35,
  half_height = 0.45
)
}
\arguments{
\item{img_dims}{Integer \code{c(nrow, ncol)}, or a single integer for a
square image.}

\item{region}{Character. One of \code{"full"} (default), \code{"eyes"},
\code{"nose"}, \code{"mouth"}, \code{"upper_face"}, \code{"lower_face"}.}

\item{centre}{Numeric \code{c(row, col)} in (0, 1) image-fraction
coordinates. Default \code{c(0.5, 0.5)}.}

\item{half_width}{Full-face ellipse horizontal half-axis as a
fraction of image width. Default \code{0.35}.}

\item{half_height}{Full-face ellipse vertical half-axis as a
fraction of image height. Default \code{0.45}.}
}
\value{
Logical vector of length \code{prod(img_dims)}, column-major.
}
\description{
Returns a logical vector of length \code{prod(img_dims)} marking an
elliptical face region (or sub-region) centred on the image. Pass to
\code{\link[=diagnose_infoval]{diagnose_infoval()}} (and \code{\link[=infoval]{infoval()}}) via the \code{mask} argument to
restrict both observed and reference Frobenius norms to the masked
region.
}
\details{
Five regions are supported:
\itemize{
\item \code{"full"} (default): the full face oval (Schmitz, Rougier, &
Yzerbyt 2024 geometry).
\item \code{"eyes"}: two small ellipses at typical eye positions.
\item \code{"nose"}: a narrow vertical ellipse along the midline.
\item \code{"mouth"}: a wide-and-short ellipse below centre.
\item \code{"upper_face"}, \code{"lower_face"}: the top and bottom halves of the
full face oval.
}

All region geometries are heuristic approximations matched to a
typical centred face on a square base image (e.g., 256x256 KDEF
male). For non-default base images, tune \code{centre}, \code{half_width},
and \code{half_height}; the sub-region geometries scale relative to that
ellipse.
}
\examples{
full <- face_mask(c(128L, 128L))
mean(full)              # ~0.49 of the image
eyes <- face_mask(c(128L, 128L), region = "eyes")
mean(eyes)              # ~0.02 of the image

}
\references{
Schmitz, M., Rougier, M., & Yzerbyt, V. (2024). Introducing the
brief reverse correlation: an improved tool to assess visual
representations. \emph{European Journal of Social Psychology}.
\doi{10.1002/ejsp.3100}
}
\seealso{
\code{\link[=diagnose_infoval]{diagnose_infoval()}}, \code{\link[=infoval]{infoval()}}.
}
